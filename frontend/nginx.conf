server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback so client-side routes work
  location / {
    try_files $uri /index.html;
  }

  # If you actually have an API, add a /api block back later and point to a real host:port
}
